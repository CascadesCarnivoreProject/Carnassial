<Window x:Class="Timelapse.DialogAboutTimelapse"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Timelapse"
        Title="About Timelapse" Height="430" Width="550"
		ResizeMode="NoResize"
		ShowInTaskbar="False"
		WindowStartupLocation="CenterOwner" BorderBrush="Black">

    <DockPanel>
        <local:StockMessage x:Name="Message" DockPanel.Dock="Top" 
                            IconType="Information" 
                            MessageTitle="About Timelapse"/>
        <StackPanel Name="StackPanelCorrect" Orientation="Vertical" DockPanel.Dock="Top" VerticalAlignment="Stretch">
            <StackPanel Orientation="Horizontal" Width="Auto" >
            <TextBlock HorizontalAlignment="Left" Margin="10,10,0,0"  VerticalAlignment="Top" DataContext="{Binding}" TextWrapping="Wrap" FontSize="12" >
                <Run FontWeight="Bold" Text="Version: " FontSize="14"/> 
                <Run x:Name="Version" FontSize="14"/>
            </TextBlock>
                <DockPanel HorizontalAlignment="Right" >
                    <Button Name="BtnVersionChanges" DockPanel.Dock="Right" Content="See Version History" Margin="10,0,0,0" Padding="5,0,5,0" Click="VersionChangesButton_Click"/>
                    <Button Name="BtnCheckForUpdate" DockPanel.Dock="Right"  Content="Check for Newer Versions" Margin="50,0,0,0" Padding="5,0,5,0" Click="CheckForUpdateButton_Click"/>
                </DockPanel>
            </StackPanel>
            <TextBlock HorizontalAlignment="Left" Margin="10,10,10,0"  VerticalAlignment="Top" DataContext="{Binding}" TextWrapping="Wrap" FontSize="12" >
                <Run FontWeight="Bold" Text="Author: " FontSize="14"/> 
                <Run Text="Saul Greenberg" FontSize="14"/>
                <LineBreak/>
                <Run Text="               Greenberg Consulting Inc. / University of Calgary " FontSize="14"/>
                <LineBreak/>
                <Run Text="               saul@ucalgary.ca" FontSize="14"/>
                <LineBreak/>
            </TextBlock>

            <TextBlock HorizontalAlignment="Left" Margin="10,10,10,0"  VerticalAlignment="Top" DataContext="{Binding}" TextWrapping="Wrap" FontSize="14" >
                <Run FontWeight="Bold" Text="Terms of use: " FontSize="14"/>
            </TextBlock>
            <TextBlock HorizontalAlignment="Left" Margin="70,0,10,0"  VerticalAlignment="Top" DataContext="{Binding}" TextWrapping="Wrap"  FontSize="14">
                <Run Text="Timelapse software is supplied 'as is' as part of a volunteer project. "/>
                <LineBreak/>
                <LineBreak/>  
                <Run Text="However, we are quite interested in making it better for you. "/>
                <LineBreak/>              
                <Run Text="Email Saul with any comments, problems and suggestions for improvements."/>
                <LineBreak/>
            </TextBlock>
        </StackPanel>

        <DockPanel Name="DockPanelImage">
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Name="OkButton"  IsDefault="True" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Padding="10" Margin="10,10,10,10" Click="OkButton_Click" IsEnabled="True">Okay</Button>
            </StackPanel>
        </DockPanel>
    </DockPanel>

</Window>

